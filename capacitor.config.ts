import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spoxx.rocket',
  appName: 'Rocket App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
